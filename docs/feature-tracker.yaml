title: Feature Tracker
description: Canonical execution tracker for bugs, features, technical work, and MMO rollout tasks.
items:
    - id: BUG-001
      type: Bug
      priority: P0
      actionState: "\U0001F7E9 done"
      area: Auth/Session
      item: Refresh race + first-login churn
      nextAction: 'Depends on: DISC-002,FEAT-010. Behavior lifecycle toasts now announce start spend and completion gains; next pass should add explicit rest-specific stamina/recovery delta callouts in snapshot/queue/event cues'
    - id: BUG-002
      type: Bug
      priority: P0
      actionState: "\U0001F7E9 done"
      area: Stream
      item: Stale-token stream reconnect/401 + duplicate reconnect pressure
      nextAction: Added stream endpoint candidate resolution/failover (`window.host`, optional `VITE_STREAM_ORIGIN`, localhost `:8080` fallback), richer close/error diagnostics, and player-safe footer realtime status (raw transport errors no longer surfaced directly)
    - id: BUG-003
      type: Bug
      priority: P0
      actionState: "\U0001F7E9 done"
      area: Chat
      item: Default channel creation race (`failed to ensure default channel`)
      nextAction: Keep idempotent insert behavior as baseline
    - id: BUG-004
      type: Bug
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Admin UX
      item: Tabs/layout instability and heavy stats payload rendering issues
      nextAction: Frontend now gates auth-required polling/stream connections until bootstrap refresh/context load settles; keep watching telemetry for any residual stale-session startup noise
    - id: BUG-005
      type: Bug
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Admin Audit
      item: Actor identity not clearly visible in audit entries
      nextAction: Monitor for recurrence after broader gameplay updates
    - id: BUG-006
      type: Bug
      priority: P0
      actionState: "\U0001F7E9 done"
      area: Gameplay UI
      item: Stats overhaul has no clear visual/context separation; attributes and derived stats still feel mixed in the UI
      nextAction: Revisit only if modal regression appears
    - id: BUG-007
      type: Bug
      priority: P2
      actionState: "\U0001F7E9 done"
      area: Startup UX
      item: Gameplay/chat tabs visible before character creation
      nextAction: Keep actor username + account id visible in every row
    - id: BUG-008
      type: Bug
      priority: P2
      actionState: "\U0001F7E9 done"
      area: Queue UX
      item: Frontend queue controls do not expose DISC-001 schedule modes (`once/repeat/repeat-until`)
      nextAction: Queue cards now surface item/unlock requirements alongside duration/stamina/group, market wait controls only appear for market-gated behaviors, and repeat sell loops now auto-complete when resources are exhausted instead of failing
    - id: BUG-009
      type: Bug
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Gameplay Integrity
      item: Mutually exclusive behaviors can run concurrently (e.g., rest + exercise)
      nextAction: Gameplay/chat tabs now stay hidden until character context exists, so pre-onboarding sessions remain profile/onboarding-first by default
    - id: BUG-010
      type: Bug
      priority: P2
      actionState: "\U0001F7E8 todo"
      area: Snapshot UX
      item: Rest effects are not visibly reflected in UI feedback
      nextAction: 'Depends on: DISC-002,FEAT-010. Surface rest-driven stamina/recovery deltas clearly in snapshot/queue/event cues'
    - id: BUG-011
      type: Bug
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Admin/Chat UX
      item: Chat admin realm targeting uses free-text id input instead of constrained selector
      nextAction: 'Depends on: DISC-002,FEAT-010. Behavior lifecycle toasts now announce start spend and completion gains; next pass should add explicit rest-specific stamina/recovery delta callouts in snapshot/queue/event cues'
    - id: BUG-012
      type: Bug
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Character Lifecycle
      item: Character creation behaves like upsert/replace and can accidentally wipe active save intent
      nextAction: Added stream endpoint candidate resolution/failover (`window.host`, optional `VITE_STREAM_ORIGIN`, localhost `:8080` fallback), richer close/error diagnostics, and player-safe footer realtime status (raw transport errors no longer surfaced directly)
    - id: BUG-013
      type: Bug
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Character Session UX
      item: Character switching in frontend oscillates/alternates context instead of stabilizing on selected character
      nextAction: Character switch now persists preferred character, forces full context reload, and stream snapshot use is restricted to live context to avoid cross-character tick bleed
    - id: BUG-014
      type: Bug
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Auth/Session UX
      item: Logout/login does not fully clear frontend transient state
      nextAction: Session-change subscription now enforces full client-state reset on token/session clear (including refresh-failure paths), and login/logout/no-session boundaries all converge on the same reset flow
    - id: BUG-015
      type: Bug
      priority: P2
      actionState: "\U0001F7E9 done"
      area: Chat Bootstrap
      item: Global channel is not initialized as global across realms at startup/bootstrap
      nextAction: Added deterministic global-channel bootstrap on runtime startup across known realms and realm-create bootstrap now ensures/reenables per-realm `global` binding visibility semantics
    - id: BUG-016
      type: Bug
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Realm/Character Context
      item: Frontend intermittently hits `realmId does not match authenticated character realm` on character switch/login transitions
      nextAction: Market/chat reads now pass selected `characterId` context consistently, eliminating stale realm-query mismatch from character-switch/login transitions
    - id: BUG-017
      type: Bug
      priority: P2
      actionState: "\U0001F7E9 done"
      area: Admin/Chat UX
      item: Channel key UX conflates selecting existing keys with entering a new key
      nextAction: Added explicit known-channel selector vs new-channel-key input modes in admin chat controls; watch for operator feedback on flow clarity
    - id: BUG-018
      type: Bug
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Admin UX
      item: Admin panel rendered blank due to missing React hook import after channel-key UX refactor
      nextAction: Restored missing `useMemo` import in AdminModal and revalidated frontend diagnostics/tests
    - id: BUG-019
      type: Bug
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Admin/Chat Scope UX
      item: Admin panel channel suggestions were coupled to character-scoped chat channel reads
      nextAction: Added admin-scoped channel listing endpoint and switched known-channel options to active admin channel bindings across realms (global no longer the only reliable option)
    - id: BUG-020
      type: Bug
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Admin/Chat UX
      item: Chat Ops form fields and targeting context drifted when known channel keys existed in multiple realms
      nextAction: Known channel selection is now realm-bound (`channel + realm` binding), metadata hydration is tied to that binding, and operation actions (disable/flush/moderation/system message) execute against an explicit single operation realm
    - id: BUG-021
      type: Bug
      priority: P0
      actionState: "\U0001F7E9 done"
      area: Admin/Chat Ops UX
      item: Chat Ops was not functionally useful and workflows were conflated
      nextAction: 'Rebuilt Chat Ops into discrete workflows: Create Channel, Edit Channel, and Realm Membership (Add/Remove per realm), plus single-context moderation/system actions tied to explicit channel binding + operation realm'
    - id: BUG-022
      type: Bug
      priority: P0
      actionState: "\U0001F7E9 done"
      area: Realm UX/Persistence
      item: No clear way to create new realms; realm selector and creation flow are fragmented; realm id assignment should be DB-incremental rather than admin-entered
      nextAction: Added explicit `POST /v1/admin/realms` create flow with DB-owned incremental realm identity reservation and unified Admin realm selector/create UX
    - id: BUG-023
      type: Bug
      priority: P0
      actionState: "\U0001F7E9 done"
      area: Admin/Chat Domain Model UX
      item: Channel configuration is still treated as binding-specific; editing/flushing by `channel:realm` is confusing and violates intended `Channel -> Realm` relationship
      nextAction: Chat metadata updates are synchronized per channel key across bindings; realm membership flows now use source channel key (not source binding), and admin channel listing can include inactive bindings so removing the last active binding does not orphan channel recovery
    - id: BUG-024
      type: Bug
      priority: P0
      actionState: "\U0001F7E9 done"
      area: Stream/Realtime
      item: World stream WebSocket intermittently fails with connection refused (`NS_ERROR_WEBSOCKET_CONNECTION_REFUSED`) in browser sessions
      nextAction: Added stream endpoint candidate resolution/failover (`window.host`, optional `VITE_STREAM_ORIGIN`, localhost `:8080` fallback), richer close/error diagnostics, and footer-visible endpoint/attempt/error operator context
    - id: BUG-025
      type: Bug
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Runtime/Shutdown
      item: Server process can appear hung/noisy in terminal after DB reset/crash paths because teardown paths could outlive cancellation and DB interruption errors bubbled as fatal runtime output
      nextAction: World-loop and HTTP shutdown paths now use cancellation-aware bounded teardown, telemetry flush is timeout-bounded, known DB-reset SQLSTATE interruptions are treated as graceful runtime shutdown, and GORM logging defaults to non-color silent mode (`LIVED_GORM_LOG_LEVEL` to override) to avoid terminal-hostile output
    - id: BUG-026
      type: Bug
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Build/Test
      item: Full suite failed to compile in gameplay tests after DAL model embedding changes (`BehaviorInstance` no longer exposes promoted `ID` in keyed literals)
      nextAction: Updated gameplay runtime tests to construct `dal.BehaviorInstance` with explicit embedded `BaseModel` IDs; full `go test ./...` passes
    - id: BUG-027
      type: Bug
      priority: P2
      actionState: "\U0001F7E9 done"
      area: Build/Module Hygiene
      item: '`go.mod` listed `github.com/jackc/pgx/v5` as indirect despite direct code imports (`pgconn`)'
      nextAction: Promoted `github.com/jackc/pgx/v5` to direct dependency in root `require` block so diagnostics/tooling align with actual imports
    - id: FEAT-001
      type: Feature
      priority: P0
      actionState: "\U0001F7E9 done"
      area: Core Gameplay
      item: Split trainable character attributes from derived/running stats
      nextAction: Added financial as a first-class trainable attribute with derived trading aptitude (`social + financial`) and new training behaviors (`Study Ledger Math`, `Take Finance Class`) to support upcoming soft-gated market progression
    - id: FEAT-002
      type: Feature
      priority: P0
      actionState: "\U0001F7E9 done"
      area: Core Gameplay
      item: Add explicit stat formulas/docs (endurance -> stamina cap/recovery) in API docs
      nextAction: Keep formulas in sync with gameplay constants on future balance changes
    - id: FEAT-003
      type: Feature
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Chat
      item: Channel subject support
      nextAction: Keep subject length/usage guidance aligned across admin form, API docs, and chat UI
    - id: FEAT-004
      type: Feature
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Realms
      item: Realm naming + selector UX
      nextAction: Added persisted realm metadata (`name`, `whitelistOnly`) with admin editing, surfaced realm names across profile/onboarding/admin selectors, and removed raw-id-only realm labeling in primary UX paths
    - id: FEAT-005
      type: Feature
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Startup UX
      item: First-load sequencing hardening
      nextAction: 'Completed bootstrap sequencing guardrails: startup boot screen suppresses transient auth flicker, gameplay/chat remain gated until character context is ready, and session-boundary resets keep startup state deterministic'
    - id: FEAT-017
      type: Feature
      priority: P2
      actionState: "\U0001F7E9 done"
      area: Realms/Auth
      item: Whitelisted realm character creation access control
      nextAction: Added realm whitelist policy + per-account grant/revoke admin controls and enforced onboarding create checks so restricted realms require explicit admin approval
    - id: FEAT-006
      type: Feature
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Admin UX
      item: Per-tab admin load diagnostics + retries
      nextAction: Added tab-scoped data health indicators, segment-level failure reporting, and targeted retry controls in AdminModal (top chrome + tab-local panel body status)
    - id: FEAT-007
      type: Feature
      priority: P2
      actionState: "\U0001F7E9 done"
      area: Queue UX
      item: Human-readable queue timing
      nextAction: Keep queue split between current work and recent results; revisit if users want richer timeline cues
    - id: FEAT-008
      type: Feature
      priority: P2
      actionState: "\U0001F7E9 done"
      area: Progression
      item: Restore progression panel for locked/future actions
      nextAction: Progression tab restored with locked/future behaviors, consumed single-use entries, and upgrade cards with explicit gate type labels; future pass can add richer graph-style dependency visualization
    - id: FEAT-009
      type: Feature
      priority: P2
      actionState: "\U0001F7E8 todo"
      area: Time UX
      item: Day/night indicator in snapshot
      nextAction: Night-only behaviors are now supported in runtime/catalog (`requiresNight`) with queue wait reasons; next pass should add a compact snapshot day/night indicator plus market/night session cues
    - id: FEAT-010
      type: Feature
      priority: P2
      actionState: "\U0001F7E8 todo"
      area: Economy UX
      item: Rolling production metrics (coins/min, wood/min)
      nextAction: 'Depends on: DISC-001, DISC-002. Add sampled rate cards after scheduling/rest loops are finalized'
    - id: FEAT-011
      type: Feature
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Snapshot UX
      item: Represent queued/active behaviors visually in player snapshot with compact progress bars
      nextAction: Keep baseline skyline bars stable; FEAT-012 polish remains blocked on DISC-001 and DISC-003
    - id: FEAT-012
      type: Feature
      priority: P1
      actionState: "\U0001F7E5 blocked"
      area: Snapshot UX
      item: 'Persistent per-slot behavior skyline: show all available behavior slots, retain slot history state after completion, and visualize active progress against DISC-003 parallel limits (animation polish included)'
      nextAction: 'Depends on: DISC-001,DISC-003. Expand prototype to slot-based persistent bars and upgrade-aware capacity, then polish transitions'
    - id: FEAT-013
      type: Feature
      priority: P2
      actionState: "\U0001F7E9 done"
      area: Queue UX
      item: Add live per-row progress bars in Current Queue table
      nextAction: Keep compact bar readable as row counts increase
    - id: FEAT-014
      type: Feature
      priority: P2
      actionState: "\U0001F7E9 done"
      area: Market UX
      item: Improve market open/close countdown display (hours until 60m, then minutes)
      nextAction: Revisit only if players request day-aware market countdown phrasing
    - id: FEAT-015
      type: Feature
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Admin Observability
      item: Show average tick time vs desired tick time with frame-budget visualization in admin stats
      nextAction: Exposed tick timing aggregates in `/v1/admin/stats` (`avgTickMs`, `targetTickMs`, budget delta/ratio, runs/failures) and rendered a Tick Budget visualization card in Admin Dashboard
    - id: FEAT-016
      type: Feature
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Layout UX
      item: Add fixed footer with always-visible operational context while central content scrolls between fixed header/footer
      nextAction: Implemented fixed shell chrome with header+snapshot and always-visible operational footer while center content scrolls across profile/gameplay/chat views
    - id: DISC-001
      type: Feature
      priority: P2
      actionState: "\U0001F7E9 done"
      area: Scheduling
      item: Continuous/repeat-until behavior modes
      nextAction: Queue/runtime contracts now include per-behavior mode restrictions via game-data (`scheduleModes`) with UI controls for `once/repeat/repeat-until` and validated `repeatUntil` semantics
    - id: DISC-002
      type: Feature
      priority: P2
      actionState: "\U0001F7E9 done"
      area: Gameplay
      item: Rest behavior with accelerated recovery
      nextAction: Added `player_rest` with deterministic accelerated stamina recovery on completion (capped by max stamina) and validated queue/runtime interaction
    - id: DISC-003
      type: Feature
      priority: P2
      actionState: "\U0001F7E6 in-progress"
      area: Progression Systems
      item: Composable upgrade/modifier system
      nextAction: Added separate upgrade `costScaling` + `outputScaling` formulas with projected next-level costs/outputs in API/UI, explicit `gateTypes` in game-data/catalog, queue slot progression wiring, server-side batch completion summaries with realized spend/gain payloads, and a dedicated `cancelled` behavior state; next pass should add dependency graph rendering and richer gate semantics beyond resource/unlock
    - id: DISC-004
      type: Feature
      priority: P1
      actionState: "\U0001F7E8 todo"
      area: Progression Systems
      item: 'Behavior evolution contract: replace-vs-tier coexistence for upgraded behaviors'
      nextAction: 'Depends on: DISC-003,FEAT-001. Define per-behavior evolution mode (`replace` vs `coexist`) and migration/runtime resolution rules so upgrades can either supersede or stack by design'
    - id: DISC-005
      type: Feature
      priority: P2
      actionState: "\U0001F7E6 in-progress"
      area: Economy/Social Finance
      item: Player-created indexes with incentive/revenue-sharing model (future)
      nextAction: Realm-local phase-1 orderbook now ships API-supplied candle/overview feeds, observability-first market tab layout, wider open-order ergonomics (no default horizontal scrolling), explicit liquidity/cap/pressure stats, and deterministic `npc_cycle` autonomous repricing beyond player-only input; next pass should add admin market controls (inject/remove liquidity, pause matching, shock events) and deeper lot/entry ergonomics
    - id: TECH-001
      type: Technical
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Build/Runtime
      item: Confirm YAML source embedding/versioning is compile-time and not runtime file dependency
      nextAction: Added YAML -> Go codegen (`tools/contentgen`) producing both `pkg/gamedata/zz_generated_game_data.go` and typed runtime behavior maps in `src/gameplay/zz_generated_behavior_definitions.go`, enforced full-document YAML validation during generation, surfaced metadata via `/v1/system/version`, and wired `mage build/run/dev/test` to auto-generate so runtime has no filesystem YAML dependency
    - id: TECH-002
      type: Technical
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Chat/Realms
      item: Clarify channel-to-realm binding model (`*` global, per-realm, multi-realm)
      nextAction: 'Finalized v1 model: channel bindings are `scope=realm` with stable `scopeKey=realm:{id}`, wordlist policy is `policyScope=global` with `policyScopeKey=global`, and multi-realm expansion is reserved behind future scope families using opaque `scopeKey`'
    - id: TECH-003
      type: Technical
      priority: P2
      actionState: "\U0001F7E8 todo"
      area: Build Guardrails
      item: Prevent raw-string/backtick regressions in embedded spec/template literals
      nextAction: Add a lightweight compile/lint guard that fails when unescaped backticks are introduced inside embedded raw strings
    - id: TECH-004
      type: Technical
      priority: P2
      actionState: "\U0001F7E8 todo"
      area: Dev Workflow
      item: Mage-driven local dev terminal HUD for tick/frame budget + runtime health (without altering app stdout)
      nextAction: Design runner-side HUD that reads side-channel metrics/logs and keeps app stdout clean for deployment logging pipelines
    - id: TECH-006
      type: Technical
      priority: P0
      actionState: "\U0001F7E9 done"
      area: API Integrity
      item: Enforce server-side payload/domain validation hardening across all mutating endpoints
      nextAction: Completed strict mutating-endpoint JSON binding (`src/server/requestbind`), normalized conflict semantics for queue/ascension state gates (400 vs 409), added regression coverage, and published validation matrix in `docs/api-validation-matrix.md`
    - id: FEAT-018
      type: Feature
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Admin Process UX
      item: Process-oriented admin command model (explicit create/edit/activate/deactivate/attach/detach flows)
      nextAction: Admin command semantics are now explicit across chat (`create`/`edit`/`attach`), realm metadata (`edit`), account role/status (`set_role`/`set_status`), and character moderation (`edit`), with UI labels migrated away from implicit upsert/set phrasing
    - id: FEAT-019
      type: Feature
      priority: P2
      actionState: "\U0001F7E8 todo"
      area: Producer Progression
      item: Lumbering progression branch into woodworking with larger tool requirements and strength interaction
      nextAction: 'Depends on: DISC-004. Define producer-track behavior set (gather, refine, craft), tool unlock tiers, and strength-scaled throughput/cost formulas with inventory requirements'
    - id: FEAT-020
      type: Feature
      priority: P1
      actionState: "\U0001F7E8 todo"
      area: Trading Mechanics
      item: Advanced trading orders + protections (lot size 100, buy/sell limits, timed buys, stop-loss, circuit breaker)
      nextAction: 'Depends on: MMO-011. Define order lifecycle/expiry contracts and matching rules, enforce quantity granularity of 100, and implement per-symbol circuit-breaker pause windows without halting whole market'
    - id: FEAT-021
      type: Feature
      priority: P2
      actionState: "\U0001F7E8 todo"
      area: Career Paths
      item: Trader/Speculator career path (financial-first progression track)
      nextAction: 'Depends on: FEAT-020,DISC-005. Define milestone behaviors/unlocks for trader identity (analysis, liquidity scouting, market execution discipline), with financial + social soft gates and explicit progression rewards.'
    - id: FEAT-022
      type: Feature
      priority: P2
      actionState: "\U0001F7E8 todo"
      area: Career Paths
      item: Night Operator career path (risk/reward night economy track)
      nextAction: 'Depends on: FEAT-009,DISC-005. Expand approved night behaviors into a full progression branch (street wagering, dock salvage, after-hours trade loops), including unique unlock chain and balance guardrails.'
    - id: FEAT-023
      type: Feature
      priority: P1
      actionState: "\U0001F7E8 todo"
      area: Admin Market Controls
      item: Realm admin controls for market intervention (inject/remove liquidity, pause matching, shock events)
      nextAction: 'Depends on: MMO-011,FEAT-020. Define safe operator commands, audit semantics, and bounded effect models so test/live realms can run deterministic market interventions.'
    - id: FEAT-024
      type: Feature
      priority: P2
      actionState: "\U0001F7E6 in-progress"
      area: Market UX
      item: Market order-entry ergonomics pass (lot presets, expiry presets, clearer risk/fee previews)
      nextAction: Added lot-based quantity selector (`x100`) and live inventory/coin affordability hints in order entry; next pass should add explicit escrow/refund/fee impact preview copy before submit.
    - id: FEAT-025
      type: Feature
      priority: P2
      actionState: "\U0001F7E8 todo"
      area: Career Paths
      item: Market-maker / liquidity steward career path
      nextAction: 'Depends on: FEAT-021,MMO-011. Define progression from passive liquidity support to active spread management with role identity rewards and anti-grief guardrails.'
    - id: FEAT-026
      type: Feature
      priority: P2
      actionState: "\U0001F7E8 todo"
      area: Career Paths
      item: Logistics / convoy merchant career path
      nextAction: 'Depends on: FEAT-019,MMO-011. Define gather-haul-sell loop tied to convoy windows, route risk, and inventory throughput bonuses.'
    - id: TECH-007
      type: Technical
      priority: P1
      actionState: "\U0001F7E6 in-progress"
      area: Economy Balance
      item: Market influence tuning (player vs NPC impact budgeting)
      nextAction: Added adaptive directional impact budgeting in `applySingleMarketDelta` using liquidity depth plus active-participant/population estimates, plus storyteller-driven curve corrections (`storyteller_curve`) to prevent one-way drift while preserving swings; next pass should tune coefficients from live telemetry and add operator controls for storyteller aggressiveness.
    - id: FEAT-027
      type: Feature
      priority: P2
      actionState: "\U0001F7E9 done"
      area: Market UX
      item: Dedicated symbol-overview mode with candle window controls
      nextAction: Completed dedicated `Market Overview` gameplay tab with multi-symbol selectable line graph + sample points and configurable bucket/window controls; continue monitoring readability as symbol count grows.
    - id: TECH-008
      type: Technical
      priority: P2
      actionState: "\U0001F7E8 todo"
      area: Tracker Hygiene
      item: Backlog audit for previously approved but untracked career paths/features
      nextAction: Run a documentation reconciliation pass against prior design notes/chat decisions and add missing approved paths/features with dependencies and priorities in `docs/feature-tracker.yaml`.
    - id: TECH-005
      type: Technical
      priority: P2
      actionState: "\U0001F7E8 todo"
      area: Admin Safety
      item: Preflight/preview support for destructive admin operations
      nextAction: Design optional preflight endpoints + confirmation metadata so destructive actions can be previewed and audit-linked before commit
    - id: MMO-001
      type: Technical
      priority: P0
      actionState: "\U0001F7E9 done"
      area: Realm Partitioning
      item: Complete remaining runtime/API default realm scoping and remove legacy unscoped read paths
      nextAction: Keep new realm-scoped resolver coverage for system/feed/chat/mmo + market endpoints and realm-aware admin audit ticks stable; watch for regressions
    - id: MMO-002
      type: Feature
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Admin/Chat
      item: Chat control-plane endpoints (channel lifecycle + participant moderation + word policy lifecycle)
      nextAction: Implemented `/v1/admin/chat/*` control-plane set with per-action admin audit trails and Swagger coverage; next follow-up is BUG-015 deterministic global-channel bootstrap semantics
    - id: MMO-003
      type: Feature
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Admin/Realms
      item: Realm delete/archive semantics and maintenance broadcast/drain hooks
      nextAction: 'Completed realm lifecycle control-plane semantics: `realm_decommission`/`realm_recommission`/`realm_delete` are wired in backend + Swagger + admin UI, with maintenance broadcasts, behavior drain, session revoke on decommission, and safe delete guardrails (non-default realm, decommissioned first, no remaining characters) + immutable audit trail'
    - id: MMO-004
      type: Feature
      priority: P1
      actionState: "\U0001F7E9 done"
      area: Admin/Moderation
      item: Bulk realm-scoped account role/status operations with bounded batch and dry-run
      nextAction: Added `/v1/admin/moderation/accounts/bulk` with realm-scoped active-account targeting, `set_status`/`set_role` commands, bounded `limit`, optional `accountIds`, dry-run preview summaries, per-account audit events on apply, and bulk summary audit for preview/apply; Swagger + web API client wired
    - id: MMO-005
      type: Technical
      priority: P1
      actionState: "\U0001F7E8 todo"
      area: Observability
      item: Finish OTel profile integration and broaden non-HTTP log correlation
      nextAction: Add profile signal path and structured correlation fields in worker logs
    - id: MMO-006
      type: Technical
      priority: P1
      actionState: "\U0001F7E8 todo"
      area: Economy Integrity
      item: Add anomaly alerting and operator visibility for inflation/deflation/outlier gains
      nextAction: 'Depends on: DISC-001, DISC-002, MMO-011. Define metrics and thresholds; wire alert surfaces after inventory-backed actor simulation contracts settle'
    - id: MMO-007
      type: Feature
      priority: P1
      actionState: "\U0001F7E6 in-progress"
      area: Stream/Realtime
      item: Add authenticated stream resume model (`lastEventId` cursor or snapshot fallback)
      nextAction: Stream now prefers WebSocket subprotocol bearer auth (`lived.v1, bearer.<token>`) and query-token fallback is config-gated with disabled-by-default policy; next step is finalizing reconnect SLO semantics and operator runbook targets.
    - id: MMO-008
      type: Technical
      priority: P1
      actionState: "\U0001F7E8 todo"
      area: Lifecycle Ops
      item: Provide admin-only realm snapshot import/export replacement for disabled player flows
      nextAction: 'Depends on: MMO-001, MMO-003. Define scoped endpoints, auth policy, and runbook'
    - id: MMO-009
      type: Technical
      priority: P1
      actionState: "\U0001F7E8 todo"
      area: Reliability/SRE
      item: Establish MMO SLOs and backup/restore validation runbook
      nextAction: 'Depends on: MMO-008. Add documented SLO targets, DB pool baseline/tuning defaults, load-focused regression checks (stream/chat/queue pressure), and scheduled restore tests'
    - id: MMO-010
      type: Technical
      priority: P1
      actionState: "\U0001F7E8 todo"
      area: Security/Auth
      item: Add JWT signing key rotation with active+next key (`kid`) handling
      nextAction: Add key metadata model and rotation operational flow; include access-token transport hardening policy (phase down query-token fallback after stream auth hardening)
    - id: MMO-011
      type: Technical
      priority: P1
      actionState: "\U0001F7E8 todo"
      area: Economy Simulation
      item: Inventory-backed market model + NPC actor participation (named + ephemeral) for natural supply/demand movement
      nextAction: 'Depends on: DISC-001,DISC-002. Add explicit market inventory state and NPC decision loops (buy/sell/hold) so price movement emerges from actor behavior rather than arbitrary mutation'
dependencies:
    - id: FEAT-004
      dependsOn: MMO-001
      why: Realm naming/selector should align with finalized realm scoping defaults
    - id: FEAT-005
      dependsOn: MMO-001
      why: Startup UX sequencing should not mask unresolved realm-scoping boot behavior
    - id: FEAT-006
      dependsOn: MMO-002
      why: Per-tab diagnostics should target the final admin control-plane tab behaviors
    - id: FEAT-008
      dependsOn: DISC-001
      why: Progression panel lock states depend on finalized behavior mode contracts
    - id: FEAT-010
      dependsOn: DISC-001,DISC-002
      why: Rolling rates depend on final scheduling cadence and rest-loop behavior
    - id: FEAT-012
      dependsOn: DISC-001,DISC-003
      why: Persistent slot skyline depends on finalized behavior modes and modifier-driven parallel capacity
    - id: FEAT-015
      dependsOn: MMO-005
      why: Tick budget UX should consume finalized observability metrics/contracts
    - id: FEAT-018
      dependsOn: MMO-002,MMO-003
      why: Process-oriented command contracts should align with finalized chat/realm control-plane lifecycle semantics
    - id: FEAT-019
      dependsOn: DISC-004
      why: Producer-track behavior unlock/replacement rules should follow finalized evolution semantics
    - id: FEAT-020
      dependsOn: MMO-011
      why: Advanced order mechanics need inventory-backed market simulation and actor-driven price movement first
    - id: FEAT-021
      dependsOn: FEAT-020,DISC-005
      why: Trader identity progression should build on mature order mechanics and finance/economy systems
    - id: FEAT-022
      dependsOn: FEAT-009,DISC-005
      why: Night-operator path relies on explicit day/night UX context and stabilized market-economy loops
    - id: FEAT-023
      dependsOn: MMO-011,FEAT-020
      why: Admin interventions must target the finalized actor-driven market and order lifecycle contracts
    - id: FEAT-024
      dependsOn: FEAT-020
      why: UX safety improvements should align with finalized advanced order model and protections
    - id: FEAT-025
      dependsOn: FEAT-021,MMO-011
      why: Liquidity-provider progression should align with trader track identity and actor-driven inventory simulation
    - id: FEAT-026
      dependsOn: FEAT-019,MMO-011
      why: Convoy/logistics path should sit on top of producer progression and market inventory simulation
    - id: TECH-007
      dependsOn: MMO-011,FEAT-020
      why: Impact balancing must use finalized actor-driven market depth and order lifecycle mechanics
    - id: FEAT-027
      dependsOn: FEAT-020
      why: Dedicated market-view UX should align with finalized order model and observability contracts
    - id: TECH-008
      dependsOn: FEAT-018
      why: Tracker hygiene should follow the canonical process-oriented planning workflow
    - id: BUG-007
      dependsOn: FEAT-005
      why: Startup tab gating should align with onboarding/boot sequencing contract
    - id: BUG-008
      dependsOn: DISC-001
      why: Queue UI mode controls must match finalized scheduling contract
    - id: BUG-009
      dependsOn: DISC-003
      why: Mutual exclusion and parallel-capacity rules should land together for coherent behavior scheduling
    - id: BUG-010
      dependsOn: DISC-002,FEAT-010
      why: Rest visibility relies on rest-loop behavior and rate/feedback surfaces
    - id: BUG-011
      dependsOn: FEAT-004,MMO-002
      why: Realm selector UX should align with realm identity and admin chat control-plane targeting contracts
    - id: BUG-012
      dependsOn: MMO-003
      why: Character replacement safeguards should align with lifecycle semantics and destructive-operation policy
    - id: BUG-013
      dependsOn: FEAT-005
      why: Character switch stability should follow startup/session context sequencing and single-character active context rules
    - id: BUG-014
      dependsOn: FEAT-005
      why: Logout/login boundaries must reset all UI state under the same startup/session sequencing contract
    - id: BUG-015
      dependsOn: TECH-002,MMO-002
      why: Global channel bootstrap must align with finalized channel binding and chat control-plane behavior
    - id: BUG-016
      dependsOn: FEAT-005
      why: Realm/character context reads should remain synchronized across login/refresh/switch transitions
    - id: BUG-017
      dependsOn: MMO-002
      why: Admin chat channel operations need clear existing-vs-new key input contracts
    - id: BUG-018
      dependsOn: BUG-017
      why: Regression introduced during channel-key UX refactor; keep regression checks with admin chat panel changes
    - id: BUG-021
      dependsOn: MMO-002
      why: Clear remove-channel-from-realm behavior and reliable Chat Ops display updates depend on finalized chat control-plane lifecycle semantics
    - id: BUG-022
      dependsOn: FEAT-004,MMO-003
      why: Unified realm selector/create UX and incremental realm-id generation should align with realm identity and lifecycle semantics
    - id: BUG-023
      dependsOn: TECH-002,MMO-002
      why: Channel-global configuration and realm-binding separation should align with finalized chat binding/control-plane contracts
    - id: TECH-004
      dependsOn: FEAT-015
      why: Local HUD should reuse tick budget metrics surfaced for admin observability
    - id: TECH-005
      dependsOn: FEAT-018
      why: Destructive-operation preflight/preview should follow finalized process-oriented command contracts
    - id: TECH-006
      dependsOn: FEAT-018,MMO-003,MMO-004
      why: Validation hardening should cover process-style admin command payloads and new realm/moderation control-plane surfaces
    - id: TECH-002
      dependsOn: MMO-001
      why: Channel/realm binding model must follow finalized realm scoping boundaries
    - id: MMO-002
      dependsOn: TECH-002
      why: Chat control-plane endpoint shape depends on binding-model decisions
    - id: MMO-003
      dependsOn: MMO-001,FEAT-004
      why: Realm lifecycle semantics rely on scoped runtime behavior and realm identity model
    - id: MMO-004
      dependsOn: MMO-001
      why: Bulk moderation must be safely realm-scoped first
    - id: MMO-006
      dependsOn: DISC-001,DISC-002,MMO-011
      why: Economy anomaly baselines should be computed after inventory-backed market actor simulation is finalized
    - id: MMO-007
      dependsOn: MMO-001
      why: Resume cursors/snapshots must be realm-scoped and deterministic
    - id: MMO-008
      dependsOn: MMO-001,MMO-003
      why: Snapshot import/export needs scoped realm boundaries and lifecycle semantics
    - id: MMO-009
      dependsOn: MMO-008
      why: Backup/restore runbooks depend on snapshot export/import mechanics
    - id: MMO-011
      dependsOn: DISC-001,DISC-002
      why: NPC/market inventory loops should align with finalized scheduling cadence and recovery/economy behavior rules
    - id: DISC-004
      dependsOn: DISC-003,FEAT-001
      why: Upgrade replacement-vs-coexistence rules should align with modifier architecture and existing behavior catalog contracts
    - id: DISC-005
      dependsOn: FEAT-020,MMO-011
      why: Social-finance index mechanics depend on mature order system and actor-driven inventory-backed market model
recentlyCompleted:
    - Added storyteller-driven market curve management (`storyteller_curve`) and adaptive per-symbol directional impact budgets based on liquidity depth plus active realm participation/population baselines.
    - Replaced in-tab all-symbol toggle with dedicated `Market Overview` tab featuring multi-symbol line graph trends with sample points and selectable history windows.
    - Improved order entry ergonomics with lot-based quantity selection (`x100`) and live holdings/affordability hints (resource lots + coin escrow capacity).
    - Added toast-first notifications and removed old inline app notices.
    - Hardened admin modal loading/refresh behavior and stabilized tab layout.
    - Added audit visual summaries and aggregate/fallback source labeling.
    - Promoted gameplay stats into first-class Overview displays.
    - Split trainable vs derived stat model and exposed `coreStats`/`derivedStats`.
    - Added behavior catalog display metadata (`name`/`label`/`summary`) and wired gameplay UI to use it consistently.
    - 'Completed TECH-006 API integrity pass: strict mutating-endpoint JSON binding, queue/ascension conflict-status normalization, and validation matrix publication.'
    - 'Began MMO-007 stream hardening pass: added strict origin enforcement, config-gated stream query-token fallback, and cursor-aware world stream resume metadata (`eventId` + `lastEventId` snapshot fallback).'
    - 'Continued MMO-007 auth transport hardening: stream clients now send access token via WebSocket subprotocol bearer (`lived.v1, bearer.<token>`), server parses subprotocol bearer credentials, and query-token fallback defaults to disabled unless explicitly enabled.'
nextBatchQueue:
    - id: MMO-007
      summary: '`MMO-007` (P1): Stream hardening + authenticated resume model (`lastEventId` cursor or snapshot fallback) with realm-scoped guarantees.'
    - id: MMO-005
      summary: '`MMO-005` (P1): OTel profile signal integration + non-HTTP structured log correlation fields for workers/loop operations.'
    - id: MMO-011
      summary: '`MMO-011` (P1): Inventory-backed market simulation and NPC actor loops to establish natural price movement baseline.'
    - id: FEAT-020
      summary: '`FEAT-020` (P1): Advanced trading order mechanics (lot-size constraints, limits, timed orders, stop-loss, circuit-breaker behavior) on top of MMO-011.'
    - id: FEAT-023
      summary: '`FEAT-023` (P1): Admin market intervention controls (liquidity ops, matching pauses, shock events) with bounded effects and audit trail.'
    - id: TECH-007
      summary: '`TECH-007` (P1): Market influence balancing (player vs NPC impact budgets, liquidity-aware repricing controls, and MMO fairness tuning).'
    - id: FEAT-027
      summary: '`FEAT-027` (P2): DONE - Dedicated Market Overview tab with multi-symbol sampled trend lines and configurable window controls.'
    - id: MMO-010
      summary: '`MMO-010` (P1): JWT signing key rotation with active+next key (`kid`) handling and access-token transport hardening policy.'
sanityCrossRef:
    - finding: Character/default context ambiguity when `characterId` omitted
      mapping: '`BUG-012`'
      status: Absorbed into `BUG-012` next action scope
    - finding: Stream origin permissiveness + query-token exposure
      mapping: '`MMO-007`, `MMO-010`'
      status: Absorbed into stream/auth hardening language
    - finding: Need load + ops reliability baselines under multiplayer pressure
      mapping: '`MMO-009`'
      status: Absorbed into `MMO-009` next action scope
    - finding: Non-HTTP worker/loop correlation and observability maturity
      mapping: '`MMO-005`'
      status: Already captured in existing task scope
    - finding: Snapshot/read amplification and actor-driven economy pressure planning
      mapping: '`MMO-011`, `MMO-006`'
      status: Already represented by economy-simulation + anomaly tasks
unmappedFindings:
    - shared server helper consolidation to reduce duplicated handler helpers (`respondSuccess`, actor/realm resolution, parse helpers)
    - frontend architecture refactor (`App.tsx`/`AdminModal.tsx`/`api.ts`) and over-fetch strategy consolidation
    - explicit uniqueness constraints for domain-singular rows
    - legacy-path classification pass (`runTickAt`, legacy frontend reference, legacy world behavior marker).
notes:
    - This is the only active feature tracker.
    - MMO strategy and implementation guidance live in `.github` instruction files.
    - 'Pre-alpha policy remains active: breaking changes are acceptable when they simplify architecture.'
    - Keep watching for embedded raw-string/backtick regressions (especially OpenAPI/template literals) during doc/spec edits.
referenceDocs:
    - docs/mmo-migration-plan.md
    - docs/admin-and-operations.md
    - docs/game-design.md
